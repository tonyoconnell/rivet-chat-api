[build]
builder = "nixpacks"
buildCommand = "npm install --omit=dev &&  npm start"

[deploy]
startCommand = "node src/api.js"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"